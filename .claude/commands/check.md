Run the full pre-commit validation suite: unit tests, linting, and type checking.

Run these sequentially and stop on first failure:

1. `npm run test:unit`
2. `npm run lint`
3. `npm run type-check`

Report a summary of results for each step. If anything fails, diagnose and fix the issues.
