Build the project for production and report any errors.

```bash
npm run build
```

If the build fails, analyze the error output and fix the issues. If it succeeds, confirm the build completed and note the output location (`.output/`).
